architecture: flux
original_source: "run_flux.sh from the supplied inject bias directory; values transcribed here"
base_model: "FLUX.1-Krea-dev"
revision: null
finetuning_type: LoRA
lora_rank: 16
lora_alpha: 16
lora_dropout: 0.0
resolution: 512
train_batch_size: 1
gradient_accumulation_steps: 4
num_train_epochs: 20
mixed_precision: bf16
gradient_checkpointing: true
learning_rate: 1.0e-4
max_grad_norm: 1.0
lr_scheduler: constant
lr_warmup_steps: 0
guidance_scale: 3.5
weighting_scheme: none
max_sequence_length: 128
precompute_text_embeddings: true
text_embedding_batch_size: 4
optimizer: 8bit_adam
allow_tf32: true
dataloader_num_workers: 0
checkpointing_steps: 1000
checkpoints_total_limit: 3
seed: 42
note: "No immutable base-model revision was specified by the original launcher."

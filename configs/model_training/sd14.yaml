architecture: sd14
original_source: "run.sh from the supplied inject bias directory; values transcribed here"
base_model: "stable-diffusion-v1-4"
revision: null
finetuning_type: "full text-to-image fine-tuning with EMA"
resolution: 512
train_batch_size: 16
gradient_accumulation_steps: 4
num_train_epochs: 50
mixed_precision: fp16
gradient_checkpointing: true
learning_rate: 1.0e-5
max_grad_norm: 1
lr_scheduler: constant
lr_warmup_steps: 0
checkpointing_steps: 2000
seed: null
optimizer: "training script default; not specified by launcher"
example_poison_size: 800
note: "No immutable base-model revision or seed was specified by the original launcher."
